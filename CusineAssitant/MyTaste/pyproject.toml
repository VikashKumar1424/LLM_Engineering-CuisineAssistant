[project]
name = "mytaste"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.26.0",
    "ipykernel>=7.3.0",
    "openai>=3.6.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
mytaste = "mytaste:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
